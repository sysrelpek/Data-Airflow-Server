```markdown
# Production Operations (Daily Admin)

Daily commands for managing the live Data Airflow Server.

## Quick start commands

```bash
# Go to project root
cd /home/etluser/ai-projects/data_airflow_server

# Load production environment
set -a
source .env.prod
set +a